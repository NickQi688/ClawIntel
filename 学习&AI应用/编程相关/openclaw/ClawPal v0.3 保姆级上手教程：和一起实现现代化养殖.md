---
title: "ClawPal v0.3 保姆级上手教程：和一起实现现代化养殖"
source: "https://x.com/CryptoStwith/status/2026287372393193553"
author:
  - "[[Unknown]]"
published: 2026-02-24
created: 2026-02-25
description: "Always Bullish dca/hodl ceo/cto alpha/#100xgems / member of @33daoweb3 nfa. $EOSE $BE #美股"
tags:
  - "clippings"
---
养🦞人应该都有过类似的体验：想给🦞换个模型，打开一个几百行的 JSON 文件翻了半天找不到改哪里。好不容易找到了，改完一个逗号没注意，整个配置炸了。想让🦞自己改？改完发现它把自己人格文件覆盖了，灵魂出窍。

ClawPal 就是来终结这些痛苦的。一个桌面端的 OpenClaw 可视化管理工具，点点鼠标就能完成以前要手搓 JSON 的所有操作。而且所有改动都有 diff 预览和一键回滚，再也不怕改坏。

v0.3 是发布以来的一次大改版：**UI 全面优化**，**定时任务看门狗**、**备用模型、****🦞****help****🦞**等实用功能。这篇带你从零走一遍，包括我自己踩过的坑和一些实际使用思路。

> 官网下载：[clawpal.xyz](https://clawpal.xyz/) ｜ 免费开源，macOS / Windows / Linux 全平台

## 连接你的 OpenClaw 🦞

打开 ClawPal，顶部有连接栏。本机装了 OpenClaw 的话，会自动出现一个绿色的「本地」标签，点一下就能管理。

更常见的场景是你的 OpenClaw 跑在另一台机器上：Mac Mini、VPS、树莓派，或者某台 24 小时开着的旧电脑。这时候需要 SSH 连接。

**添加远程实例**

点顶部的 **\+ SSH**，弹出「添加远程实例」对话框

![图像](https://pbs.twimg.com/media/HB6xRFebMAA4tt0?format=jpg&name=large)

不知道怎么填？没关系，直接截图发给你的🦞

![图像](https://pbs.twimg.com/media/HB6xgtJakAEKQkU?format=jpg&name=large)

用户名，IP 不知道？都可以让🦞帮你查。

如果远程设备还没配置 SSH，弹窗底部有一个「没有 SSH 密钥？点击查看设置方法」的引导，当然最直接的方案还是让🦞帮你配，它会一步步教你。 一个实用提示：如果你家里有多台设备，推荐装个 **Tailscale**（免费），设备之间自动组网，不用折腾端口转发和公网 IP。我连 Mac Mini 用的就是 Tailscale 的内网 IP。 填好点「添加」，顶部会多一个标签。绿色圆点亮起来就表示连上了。你可以同时管理多个实例，在顶部标签之间切换。

**常见报错：Failed to exec command**

这个问题值得单独说，因为遇到的人太多了。连接成功，绿灯也亮了，但首页一片空白。底部飘出一条红色提示：

> 无法加载远程 Agent：Failed to exec command: the remote command could not be executed

![图像](https://pbs.twimg.com/media/HB6yAfFa8AAysY8?format=jpg&name=large)

作为一个正常的 OpenClaw 用户，当然是不会 debug 的，此时只需求助于我万能的🦞，直接把报错信息发过去。

![图像](https://pbs.twimg.com/media/HB6yIL8bIAAmIjj?format=jpg&name=large)

🦞帮我分析了原因：ClawPal 通过 SSH 执行命令走的是**非交互模式**，不会加载 \`.zshrc\` 里的 PATH，所以找不到 \`openclaw\` 命令。 两分钟，问题解决。我甚至不需要理解什么是"非交互模式"，🦞就帮我搞定了。

# 首页：系统状态 + Agent 管理

连上之后，首页展示 OpenClaw 实例的基础信息：健康状态、版本号、默认模型。

## 备用模型：让🦞永不掉线

v0.3 新增了**备用模型**（Fallback），可以配多个。主模型挂了（API 出错、限流、宕机）自动切换到下一个，不用担心🦞突然失联。

我的配置是：主力用 Claude Opus，备用 Claude Sonnet 和 Gemini Flash。Opus 偶尔限流的时候，🦞会自动降级到 Sonnet 继续工作，用户体验几乎无感。

![图像](https://pbs.twimg.com/media/HB7KuAvbYAA_GbV?format=png&name=large)

## Agent 列表

下面是所有 Agent 的列表。有独立工作区的 Agent 是一级卡片，子 Agent 显示在下面。可以直接在这里切换模型、新建 Agent。

如果你只有一个 Agent，这里会比较简单。但如果你想开始玩多 Agent（后面「频道」部分会详细说），这里就是你的 Agent 调度中心。

## 修改配置：先预览，再生效

这是 ClawPal 设计得最让人安心的地方：**所有配置修改都不会立即生效。**

不管你切换了模型、加了备用模型、还是删了某个 Agent，修改后左下角会出现一个提示条：「X 项待执行变更」。两个选项：

\- **预览变更**：打开 diff 视图，红绿对比，一目了然改了什么

\- **放弃**：丢弃所有未应用的修改

确认 diff 没问题，点「应用并重启网关」才真正生效。

这意味着你可以大胆地点来点去试各种配置。只要没点「应用并重启网关」，什么都没发生。手滑了就「放弃」，干干净净。

**对比一下以前：**手动改 JSON 的时候，漏个逗号整个配置炸掉，还得从 git log 里翻上一个能用的版本。现在 ClawPal 帮你做了 diff 和回滚，这种焦虑基本消失了。

![图像](https://pbs.twimg.com/media/HB60t31aoAAk4S4?format=jpg&name=large)

![图像](https://pbs.twimg.com/media/HB60wDjbsAAd_uD?format=jpg&name=large)

## 备份和升级

首页的状态区域有版本号显示，有新版本时会提示。ClawPal 提供**先备份后升级**的一站式流程：点一下备份，确认备份成功，再点升级。

这不是可选步骤，是必须养成的习惯。我之前让🦞直接 \`npm update\` 升级 OpenClaw，结果有一次升级后配置格式变了，🦞的人格文件被重置，等于灵魂出窍。如果当时有备份，一键 Restore 就恢复了。

所有历史备份都在页面底部的 Backups 区域，可以查看每个备份的内容、一键恢复或删除。

**养成习惯：无备份不更新。**让🦞帮你升级也一样，先让它备份再动手。

## 菜谱：预制菜式的引导配置

左侧点「菜谱」。这是 ClawPal 比较有特色的功能：把常见的配置操作做成了一步步的引导流程，像菜谱一样跟着做就行。

目前内置两个菜谱：

\- **Create dedicated Agent for Channel**：为某个 Discord 频道创建专用 Agent，可以选模型、选频道，还能设置独立工作区和人格

![图像](https://pbs.twimg.com/media/HB7GcV8aoAAKnRW?format=jpg&name=large)

\- **Channel Persona**：给某个频道设置系统提示词，让🦞进这个频道时自动切换人格，算是一种轻量级的频道分治

![图像](https://pbs.twimg.com/media/HB7HwT4bsAAyZIs?format=jpg&name=large)

菜谱走完后同样会进入「待执行变更」流程，预览 diff 确认后再应用。

## 实战：用 Channel Persona 给频道设人格

拿 Channel Persona 举个例子。我专门建了一个摸鱼频道，给🦞设了个毒舌人格。

点「开始烹饪」，只有一步：选频道，写提示词。我写的大意是"你是一个毒舌损友，用阴阳怪气的方式回复所有消息，要有梗，可以嘲讽但不能真的伤人，风格参考朋友之间的互怼"。

走完后左下角出现待执行变更，预览 diff 可以看到它在配置文件的 channels 里加了一段 systemPrompt。确认没问题，应用重启，搞定。

效果：在这个频道里说"今天好累啊"，🦞会回你"累？你一个让 AI 打工的人跟我说累？"。换到其他频道，🦞又恢复正常工作模式。不需要创建新 Agent，不需要改任何其他配置，就是注入一段提示词。轻量、快速、随时可改。

**更多玩法：**

\- 给客服频道设一个"只用英文回复，语气友好专业"的人格

\- 给记账频道设"只做记账相关操作，拒绝其他请求"

\- 给某个频道设"用日语回复"（试试很好玩的）

**什么时候用菜谱？** 如果你想给某个频道配一个专门写代码的 Agent，或者配一个只说日文的客服人格，用菜谱走一遍比自己翻配置文件快得多。

## 频道：多 Agent 的核心玩法

左侧点「频道」。所有聊天渠道按类型分组：Discord、Telegram、iMessage、飞书等。

Discord 会把每个 Guild 和 Channel 都列出来，**名字直接显示**，不用再对着一堆数字 ID 猜这是哪个频道。每个 Channel 可以单独下拉选择用哪个 Agent 和模型。

OpenClaw 多 Agent 的核心价值在于：**不同频道用不同的模型，既省钱又让每个频道有最合适的能力。**

举几个例子：

\- **heartbeat 频道**：用最便宜的轻量模型。心跳检查不需要强推理能力，一天跑几十次，用 Opus 纯粹烧钱

\- **coding 频道**：用最强的模型。写代码需要强推理，这里省钱等于给自己找麻烦

\- **日常闲聊频道**：用默认 main Agent。大部分场景够用

\- **专项频道**（比如记账、摘要、监控）：用轻量模型 + 专门的 system prompt，又便宜又精准

你不需要一开始就配得很复杂。先用默认的 main Agent 跑所有频道，等发现某个频道的需求明显不同了（比如太贵或能力不够），再单独给它分配 Agent。

**成本参考：**我把 heartbeat 和 expense 两个高频低复杂度频道从 Opus 换到轻量模型后，月费用大概降了 30%，效果完全没区别。

![图像](https://pbs.twimg.com/media/HB69S0waIAATJdS?format=jpg&name=large)

## 定时任务：Cron 管理和看门狗

「定时任务」，v0.3 新增了一个**看门狗**功能，开启后会监控任务执行和网关状态，异常时自动重启。如果你的 OpenClaw 跑在无人值守的机器上（VPS、Mac Mini 放角落里），强烈建议开着。

下面是所有 Cron 任务列表，每个任务有状态灯、执行频率、上次执行时间，可以手动「触发」跑一次。点击展开能看执行记录和输出。

终于不用去 SSH 上去翻日志文件了。哪个任务失败了、什么原因，一目了然。

> 状态灯是根据日志推断的，可能跟实际情况有差异。如果🦞实际运行没问题，别纠结灯的颜色。

![图像](https://pbs.twimg.com/media/HB64S8bakAAHlg9?format=jpg&name=large)

## 历史：配置变更记录和回滚

每次配置变更自动记录，可以预览 diff，也可以回滚。

回滚不会立即生效，跟前面一样要走「预览变更 → 应用」的流程。**手滑点了回滚也不用慌**，直接「放弃」就行，什么都不会变。

这套安全机制在 ClawPal 里是一致的：所有操作都是「暂存 → 预览 → 确认」三步走，任何一步都可以反悔。

![图像](https://pbs.twimg.com/media/HB64opbbwAAt9_U?format=jpg&name=large)

## 会话管理：清理 Session 的正确姿势

OpenClaw 每次对话都会创建 Session 文件，开了 Discord autoThread 后日积月累上千个很正常。

点「分析」扫描所有 Session，按 Agent 分组，标注价值高低。

可以点开看内容，点击左侧 session id 即可，确认没用的再删。

> **注意：** 价值评估只是参考，别盲目「清理低价值」。有些短对话看起来「低价值」，但可能是你留着备忘的重要信息。建议第一次清理的时候，花几分钟把标记为低价值的 Session 扫一眼，心里有个数之后再批量操作。

![图像](https://pbs.twimg.com/media/HB646UdaoAAup5S?format=jpg&name=large)

## 设置：配置模型和 API Key

左侧点「设置」，管理 AI 服务配置。已有的模型会列出来，点「添加配置」可以加新的：填 provider、模型名、API Key。已有同 provider Key 时不用重复填。

新版支持**自定义 Base URL**，用中转服务或者自己维护模型的朋友，勾选后填地址即可。

> 用 OpenClaw 自带的 OAuth 登录过的 provider，Profile 会自动出现，不用手动加 Key。

![图像](https://pbs.twimg.com/media/HB65K23acAA0CCn?format=jpg&name=large)

## 聊天：随时询问你的🦞

每个页面右上角都有「聊天」按钮，弹出侧边面板，可以选 Agent 直接对话。独立 Session，不会跟其他渠道混在一起。

参数不会填、报错看不懂、配置不确定对不对，丢给🦞就行。这才是正确的养虾姿势：你养的是一只会帮你解决问题的🦞，不是一个需要你解决它问题的🦞。

![图像](https://pbs.twimg.com/media/HB65ZYub0AACwB6?format=jpg&name=large)

## 🦞 help 🦞

v0.3 还上线了一个叫 **🦞** **help** **🦞** 的功能：让你的一只🦞去帮另一只🦞排查修复问题。

目前能用到这个功能的前提是你有两个 OpenClaw 实例，算是比较进阶的玩法。后续 ClawPal 计划推出更易用的版本，让普通用户也能享受到虾帮虾的便利，这里先不展开了。

## 写在最后

没想到换个设备写一篇 ClawPal 配置教程会遇到那么多问题。但也正因如此，整个过程变成了一次真实的人🦞协作：我让🦞帮我查 IP、排查 SSH 报错，它来动手我来描述。遇到问题不是我去翻文档，而是直接问它，它搞定。

ClawPal 降低了操作门槛，🦞补上了认知门槛，两者配合起来，配置 OpenClaw 这件事变成了一场协作，而不是一场考试。

这大概就是「和🦞一起实现现代化养殖」的真正含义：不是工具替你做所有事，而是你和🦞一起，各出一半力，把事情搞定。

> ClawPal 免费开源，有 Bug 或需求欢迎到 Discord 反馈！ 官网：[clawpal.xyz](https://clawpal.xyz/) ｜ 作者：[@zhixianio](https://x.com/@zhixianio) Discord：[https://discord.gg/d5EdxQ8Qnc](https://discord.gg/d5EdxQ8Qnc)