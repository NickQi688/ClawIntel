# Gemini Seedance 2.0 Prompt Generator - 快速开始指南

> 创建日期：2026-02-12
> 用于快速安装和配置，分享给朋友使用

---

## 一、快速安装（3 分钟）

### 方式 1：推荐（自动安装依赖）

```bash
# 最简单！一条命令搞定
npx skills i gemini-seedance-prompt-generator
```

### 方式 2：手动克隆（GitHub 仓库）

```bash
# 1. 克隆仓库
git clone https://github.com/NickQi688/gemini-seedance-prompt-generator.git ~/.claude/skills/gemini-seedance-prompt-generator

# 2. 进入目录
cd ~/.claude/skills/gemini-seedance-prompt-generator

# 3. 完成！（见下方配置步骤）
```

---

## 二、API 配置（必須）

### 模型选择

使用 **Gemini 3.5 Flash Preview** 模型。

**为什么这个模型？**
- 100 万 tokens 上下文
- 快速生成（低延迟）
- 低成本（$0.075/百万 tokens）
- 强大的视觉理解能力

### 获取 API Key

#### 方式 A：Google AI Studio（推荐）

**优点**：
- ✅ 官方支持，稳定可靠
- ✅ 每月有免费额度（新用户 $300/200）
- ✅ 国内可直接访问（需要魔法工具）
- ✅ 专为 Gemini 优化

**步骤**：
1. 访问：https://aistudio.google.com/app/apikey
2. 登录 Google 账号
3. 创建 API Key（选择项目 → 创建 API Key）
4. 复制 API Key

**配置文件**：`~/.claude/skills/gemini-seedance-prompt-generator/config.json`
```json
{
  "model": "gemini-3.5-flash-preview",
  "maxTokens": 100000,
  "temperature": 0.7,
  "apiKey": "你的 Google AI Studio API Key"
}
```

#### 方式 B：OpenRouter（备选，国内友好）

**优点**：
- ✅ 无需科学上网即可访问
- ✅ 多模型聚合平台
- ✅ 支持 Gemini 3.5 Flash

**步骤**：
1. 访问：https://openrouter.ai/models/google/gemini-2.5-flash-preview
2. 注册账号（获取 API Key）
3. 充值（最低 $5 起步）

**配置文件**：
```json
{
  "model": "gemini-3.5-flash-preview",
  "maxTokens": 100000,
  "temperature": 0.7,
  "apiKey": "你的 OpenRouter API Key",
  "apiProvider": "openrouter"
}
```

---

## 三、验证安装

### 测试 Skill

```bash
# 在 Claude Code 中激活
gemini-seedance-prompt-generator
```

### 测试提示词生成

```
用户：帮我生成 Seedance 2.0 提示词
需求：制作餐饮视频封面
```

**预期输出**：
- 3 个版本提示词（基础版/专业版/创意版）
- 每个版本包含画面描述、运镜、音效建议
- **中英混合格式**：中文描述为主，关键术语用英文（如：Close-up shot、Warm yellow light）

---

## 四、配置文件详解

### config.json 完整示例

```json
{
  "model": "gemini-3.5-flash-preview",
  "maxTokens": 100000,
  "temperature": 0.7,
  "apiKey": "你的 API Key 放这里",
  "nanoBananaProSkill": "nano-banana-pro-prompts-recommend-skill"
}
```

### 参数说明

| 参数 | 推荐值 | 说明 |
|------|---------|------|
| `model` | "gemini-3.5-flash-preview" | 模型名称 |
| `maxTokens` | 100000 | 最大输出长度（足够生成 3 个版本） |
| `temperature` | 0.7 | 创造性（0-1，越高越随机） |
| `apiKey` | "你的 API Key" | 必填，不能为空 |
| `apiProvider` | "auto" / "openrouter" / "siliconly" | API 提供商 |
| `nanoBananaProSkill` | "nano-banana-pro-prompts-recommend-skill" | Nano Banana Pro Skill 路径（自动识别） |
| `outputFormat.language` | "mixed" | 语言模式：zh-CN=中文，en=英文，mixed=中英混合 |

### 如何切换 API 提供商

**从 Google AI Studio 切换到 OpenRouter**：

1. 修改 `apiProvider`：
   ```json
   "apiProvider": "openrouter"
   ```
2. 修改 `apiKey`：
   ```json
   "apiKey": "你的 OpenRouter API Key"
   ```
3. 保存即可

---

## 五、常见问题

### Q1：安装后找不到 Skill？

**解决方案**：
```bash
# 检查是否安装成功
ls ~/.claude/skills/gemini-seedance-prompt-generator

# 应该看到这些文件：
# - SKILL.md
# - config.json
# - package.json
# - README.md
```

### Q2：提示"API Key 不能为空"？

**解决方案**：
- 检查 `config.json` 中的 `apiKey` 字段
- 确保填入了有效的 API Key
- 保存文件后重新测试

### Q3：生成速度很慢？

**可能原因**：
- 网络问题（使用 OpenRouter 可能更稳定）
- `maxTokens` 设置过高（建议 100000 足够）
- 模型服务器负载高

**解决方案**：
- 尝试降低 `maxTokens` 到 50000
- 切换 API 提供商
- 分段生成（先生成首帧，再生成视频）

### Q4：首帧图片优化不工作？

**检查项**：
1. Nano Banana Pro Skill 是否正确安装？
   ```bash
   npx skills i nano-banana-pro-prompts-recommend-skill
   ```
2. 触发关键词是否正确？
   - "封面"、"缩略图"、"海报"

---

## 六、使用技巧

### 优化首帧提示词

**为什么要优化首帧？**
- 首帧决定整体风格和质量
- Seedance 2.0 依赖首帧建立场景基调
- 高质量首帧 → 更好的人物一致性、光影、构图

**如何工作？**
```
你的需求 → Nano Banana Pro 推荐首帧图片 → Seedance 2.0 生成视频提示词 → 生成视频
```

**实际示例**：
```
用户：做个烤肉广告封面，要温暖食欲感

Nano Banana Pro 推荐首帧：
- 烤肉特写，油光滋滋作响
- 烟雾缓缓升腾，金色阳光

Seedance 2.0 生成视频提示词（中英混合格式）：

## 基础版 Basic
- 0-3秒：特写镜头（Close-up shot），烤肉滋滋作响（Sizzling barbecue）
- 光线：暖黄光（Warm yellow light）+ 侧逆光（Rim light）
- 音效：炭火滋滋声（Charcoal sizzling sound）

## 专业版 Professional
- 0-3s：推镜头（Dolly in），焦距50mm，光圈f/2.8
- Lighting：三点布光（Three-point lighting），主光45°角（Key light at 45°）
- Audio：环境音（Ambient sound）+ 炭火声（Sizzling）
```

---

## 七、分享给朋友

### 复制配置文件

**不要直接分享你的 `config.json`**（包含个人 API Key）

**正确做法**：
1. 创建配置模板：
   ```bash
   # 复制整个 Skill 文件夹
   cp -r ~/.claude/skills/gemini-seedance-prompt-generator ~/Desktop/gemini-seedance-skill-backup

   # 或者只复制配置文件模板
   cp ~/.claude/skills/gemini-seedance-prompt-generator/config.json ~/desktop/config-template.json
   ```

2. 分享给朋友：
   - **GitHub 仓库链接**：https://github.com/NickQi688/gemini-seedance-prompt-generator
   - **安装命令**：`npx skills i gemini-seedance-prompt-generator`
   - **配置文件路径**：`~/.claude/skills/gemini-seedance-prompt-generator/config.json`
   - **获取 API Key**：分享上方文档中的链接

3. 说明配置步骤：
   - 方式 A：Google AI Studio（推荐，每月有免费额度）
   - 方式 B：OpenRouter（备选，国内友好）
   - 必须配置 `apiKey` 才能使用

4. 注意事项：
   - ⚠️ **不要分享你的 API Key**
   - ✅ 每个人需要单独申请
   - 🔒 保护好个人密钥安全

---

## 八、版本信息

- **Skill 版本**：v1.0.0
- **创建日期**：2026-02-12
- **维护者**：小鲸 & Claudian
- **适用平台**：Claude Code（macOS/Windows/Linux）

---

**祝使用愉快！有问题随时反馈。**
